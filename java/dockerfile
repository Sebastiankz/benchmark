# Usar la imagen oficial de OpenJDK (el último disponible)
FROM openjdk:latest

# Establecer el directorio de trabajo dentro del contenedor
WORKDIR /app

# Copiar todos los archivos del directorio actual al contenedor
COPY . .

# Compilar el código Java
RUN javac SpiralMatrix.java

# Ejecutar el programa después de la compilación
CMD ["java", "SpiralMatrix"]
