# Imagen base de Rust
FROM rust:latest

# Establecer el directorio de trabajo dentro del contenedor
WORKDIR /app

# Copiar los archivos del código fuente
COPY . .

# Compilar el programa en Rust
RUN rustc -o solution main.rs

# Ejecutar el binario compilado
CMD ["./solution"]
